{
  "name": "cap_todoList",
  "type": "module",
  "version": "1.0.0",
  "description": "A simple CAP project.",
  "repository": "<Add your repository here>",
  "license": "UNLICENSED",
  "private": true,
  "dependencies": {
    "@cap-js/sqlite": "^2.0.4",
    "@sap/cds": "^9.4.4",
    "@sap/cds-dk": "^9.4.3",
    "express": "^4"
  },
  "scripts": {
    "start": "cds-serve",
    "watch": "cds w",
    "build": "cds build"
  },
  "imports": {
    "#cds-models/*": "./@cds-models/*/index.js"
  },
  "cds": {
    "requires": {
      "messaging": true,
      "TodoService": {
        "kind": "local",
        "model": "srv/todolist-service"
      },
      "server": {
        "port": 4005
      }
    }
  }
}
